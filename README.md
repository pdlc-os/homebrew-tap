# pdlc-os Homebrew Tap

Casks and formulae published by [pdlc-os](https://github.com/pdlc-os).

```bash
brew tap pdlc-os/tap
```

## Casks

| Cask | Description |
|---|---|
| [`planroom`](Casks/planroom.rb) | Native macOS app for viewing and planning Microsoft Project (`.mpp`) schedules |

```bash
brew install --cask planroom
```

Casks here are generated by their source repository's release workflow on every
tag — each release substitutes the real `sha256` of the published artifact.
Hand edits will be overwritten by the next release.
